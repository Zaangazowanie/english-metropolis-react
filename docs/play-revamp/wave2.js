export const meta = {
  name: 'em-play-revamp-wave2',
  description: 'Wave 2 of the englishmetro.com/play revamp on the integrated play/wave1 base: character revamp (owner priority), server progress + leaderboard + ride + wayfinding, learning content authoring; max 3 agents',
  phases: [{ title: 'Build', detail: 'chars / quests / content lanes in parallel worktrees' }],
}

const SHARED = `
You are one of three wave-2 builder lanes on "English Metropolis", the open-world 3D English-learning game at englishmetro.com/play (vanilla three.js r182 ES modules, NO bundler, import map in public/play/index.html; source public/play/src/*.js, kit under src/kit/, data under src/gamedata/). Wave 1 (already merged into your base branch play/wave1) delivered: golden-hour daylight + toon v2 + cascaded shadows + outline/DOF composite + conversation camera + arrival dolly (render lane, see WAVE1-render.md), a facade/street/flora/landmark kit + streaming (city lane, WAVE1-city.md), and the "Metro Pass" gameplay system: first-click scoring, overlay stack, progress store (src/progress.js), ranks/stamps/certificates, bank lint, mobile HUD (game lane, WAVE1-game.md). READ those three files first, then the parts of the review that concern you: /tmp/claude-0/-root/73bb1e95-7894-4a1d-82d2-c947c0590d3c/scratchpad/REVIEW-findings.md and PLAN.md.
The owner has seen wave 1 and said two things: "I love the saturated colours now" and "look at the quality of the NPCs — absolutely terrible and generic, we need them looking way better." Aesthetic ceiling remains Abeto Studio's Messenger: lush, cel-shaded, hand-crafted, readable silhouettes, warm light.
Product rules (hard): quest NPCs are "locals", never "teachers/tutors"; game UI = v3 design system (Space Grotesk, violet glass, brand gradient, amber accent), parchment only for in-world props; nobody spawns in view; no duplicate character bodies visible; every HUD text >= 13 px; no phonetic caricature in dialect content (real lexis, standard spelling).

YOUR WORKTREE: work ONLY inside your worktree (below), on your branch, created from play/wave1 @ 99d5317. Commit early and often with clear messages (no push). Never touch /root/englishmetro, /root/em-wt-int or other lanes' worktrees. Your worktree's public/ is served on your port (edits live on reload); restart if needed: cd <worktree>/public && nohup python3 -m http.server <port> --bind 127.0.0.1 &

FILE OWNERSHIP (strict; I merge the three branches afterwards):
  chars lane owns: crowd.js, rig.js, hero.js, citizens.js, player.js Player class (FollowCamera stays as is; tiny hunks allowed), NEW src/characters/*.js, train.js + traffic.js motion/light details, world.js ONLY placeHubNPCs facing/look-at hooks, zones.js ONLY the two lines that spawn district locals' rigs (to call your new API), main.js ONLY the crowd/citizens construction lines and the wrapToonHook call.
  quests lane owns: progress.js, ui.js, overlay.js, ranks.js, index.html (HUD/overlays/CSS), minimap.js, markers.js, main.js ONLY the interaction/HUD section + rideTo (coordinate: rideTo is render's code; you extend it into a boarded ride), NEW src/net.js, src/ride.js, src/wayfinding.js.
  content lane owns: src/gamedata/* (zones.json, grammar_bank.js + .json, chatter.json), public/play/tools/*, dialects.js, and TWO small code hunks only: rendering warm-up 'explain' + optional 'pl' gloss in ui.js openDialog warm-up branch, and assignGrammar reading zones.json objective.concept in grammar.js. Every bank change must pass node public/play/tools/lint-grammar-bank.mjs with 0 offenders.
  If you must touch a line outside your region keep the hunk minimal and list it under mergeNotes.

CONSTRAINTS: no npm install, no new deps, no Blender on this box (character work is procedural geometry + shaders + animation code). three r182 vendored at public/play/public/vendor/three (+ addons GLTFLoader, DRACOLoader, BufferGeometryUtils, SkeletonUtils, meshopt). Keep everything batched/instanced; per-tier draw-call and triangle budgets: high hub <= 500 calls / 2.0 M tris including shadow passes, potato hub <= 260 / 0.8 M (wave-1 numbers are in WAVE1-render.md / WAVE1-city.md; the crowd body is ~300 k tris of the high district frame today — do not exceed 1.6x that with the new bodies). Keep window.__EM contract and localStorage keys backward compatible. Keep the signup gate.

CPU DISCIPLINE (hard rule — the host throttled this VPS to 93% CPU steal after hours of flat-out SwiftShader; every probe you run is expensive for everyone): run EVERY headless probe through the wrapper `/root/ricky-estate-2026-09-01/em-play-revamp/probe-run.sh -- node <script>.mjs ...` — it queues probes machine-wide (one at a time), caps the whole probe tree at 1.5 cores (cgroup CPUQuota) at low priority, refuses to start while host CPU steal > 40% (exit 75: do code work and retry later), and kills the tree on exit so nothing orphans. Never run node/playwright directly. The owner has asked that our work never pins the box again; this wrapper is how that promise is kept. Iterate at 960x540 and TIER=potato unless the check is about lighting/shadows; take 1440x900 TIER=high shots only as final evidence, and only for the views you will cite. Step only the frames you need (step(3-8), not 30). Also wrap in `timeout` and close the browser on SIGTERM/uncaughtException (tour-play.mjs already does). Check `sar -u | tail -2`: if steal > 40%, do code work instead and probe later. Prefer DOM/state assertions over screenshots where a screenshot is not the evidence.
CONTINUITY: if your branch already has commits or a WIP commit beyond play/wave1 (a previous run of this lane was interrupted), read them first (git log, git show --stat) and CONTINUE from that state; do not redo finished work.

VERIFY HEADLESS (SwiftShader on this VPS): tour script /tmp/claude-0/-root/73bb1e95-7894-4a1d-82d2-c947c0590d3c/scratchpad/tour-play.mjs — copy to your own name in that directory (node_modules symlink there), point at your port. It bypasses the signup wall, skips onboarding, forces a tier via TIER env, PAUSES the render loop after BEGIN (SwiftShader takes 1-3 s per full frame on the wave-1 scene) and drives frames with window.__EM.step(n); screenshots need timeout: 180000. Run ONE probe at a time (the flock above), iterate at 960x540, final shots at 1440x900. LOOK at every screenshot (Read tool). Reproduce before you fix; cite before/after artefacts. Zero pageerrors and console errors at the end. The game must load and BEGIN at every commit.

WORK HARD AND LONG. Whole files before edits. Match code style. Write <worktree>/WAVE2-<lane>.md and return the structured report.`

const LANES = [
  { key: 'chars', worktree: '/root/em-wt-chars', port: 4185, brief: `
LANE: CHARACTER REVAMP — the owner's top priority this wave. Today: the GPU crowd (crowd.js) is a cylinders-and-spheres mannequin with one flat colour per garment and a dot face; the 7 Meshy walkers (citizens.js) and 8 rigged Meshy quest locals (main.js NPC_ASSETS → zones.js/world.js) are 24 k-tri PBR models toonified with no outline, standing in one idle pose and never looking at the player; the hero (Mixamo idle/walk/run) has no jump pose, no look-at, no idle variety. Screenshots that show the problem: scratchpad/cityfinal-high-d26-us_texan-shopfront.png (crowd walker), g-high-07-hub-local-close.png, g-high-18-district-local-close.png, final-high-02-hub-hero-low.png, and the owner's own capture /root/.claude/uploads/73bb1e95-7894-4a1d-82d2-c947c0590d3c/669c329f-image.jpg.
Build, verifying each step with 3 m close-up screenshots (golden AND night, TIER=high and potato):
 1. CROWD BODY v2 (crowd.js + NEW src/characters/body.js, wardrobe.js): keep ONE InstancedMesh with gated parts (the existing technique; WebGL2 guarantees 16 vertex attributes so pack more per-instance bits into fract()/floor() of the seed floats). Stylised, readable silhouettes in the Abeto register: 5 body-type presets (height/shoulder/hip/belly), real proportions (heads ~1/7, not pinheads), rounded chunky limbs, hands with thumbs, feet with soles. Heads: 10+ hairstyles (short, side-part, bob, bun, ponytail, afro, braids/locs, long, undercut, bald) + headwear gate (cap, beanie, sun hat, flat cap, headscarf/hijab, cowboy hat, bucket hat), faces with sclera + iris + highlight, brows, mouth, blush/cheeks, optional glasses / beard / moustache / earrings. Clothing layers as geometry, not paint: tee / hoodie with hood + pocket / shirt with collar + buttons / jacket or coat with lapels and hem / dress / kurta, and trousers / shorts / skirt / jeans with cuffs; shoes vs boots vs sandals. Accessories gate: backpack, shoulder bag, tote, umbrella (up when the district is 'rainy' by climate keyword), phone in hand (arm pose), coffee cup, shopping bag, headphones. Vertex-baked shading: AO under chin, armpits, between legs, under hems; cloth fold darkening bands; two-tone garments (trim colour). WARDROBE PER DISTRICT: derive garment mix + palette from the zone (country prefix, climate/architecture keywords, zone palette): Queen's Mile tweed/trench/umbrellas, Dublin wool coats + scarves, Boston peacoats + beanies, Texan hats + denim + boots, Jamaica/Trinidad bright prints + sun hats, Cape Town/Soweto bold colours, Quebec parkas... crowd.setDistrictWardrobe(code) chosen when the district populates (zones.js populateDistrict already assigns agents per district — hook there). Skin tones broad and evenly distributed everywhere. OUTLINE: a second instanced draw of the same body with inverted hull (BackSide, normal push ~1.5 cm scaled by distance) sharing the gait shader (+1 draw call total), ink colour from the daylight uniform (materials.js EM). Budget: <= 1.6x today's crowd triangles at the same counts.
 2. GAIT + BEHAVIOUR v2 (crowd.js shader + agent logic): arm counter-swing with elbow bend, hip sway, torso lean into walk direction, head bob, stride length/cadence variance per body type; poses: phone-in-hand walk, bag-carry arm, umbrella arm; pairs walking together (two agents on adjacent lanes with synced phase, occasionally turning heads to each other); some agents pause, look at the player when the player sprints past within 3 m (head yaw toward player via one uniform + per-instance flag), wave back when the player stops in front of them for 2 s; a few sit on benches where the street kit placed them (read bench positions from the chunk's userData if exposed; if not, skip sitting and say so).
 3. HERO (hero.js, player.js Player): inverted-hull outline on the SkinnedMesh (clone bound to the same skeleton, BackSide material with normal push), head look-at toward the nearest local within 6 m or the camera when idle (rotate neck/head bones after mixer.update), idle variations every 6-10 s (weight shift, look around, stretch) via additive procedural bone offsets, landing squash + dust puff (small Points burst) on jump landing, jump pose (arms up, knees tuck) procedurally when airborne, sprint lean, turn-in-place foot shuffle. Keep the wrist constraint postUpdate.
 4. QUEST LOCALS (rig.js instanceRig + main.js NPC bases + zones.js/world.js hooks): outline hulls on every rigged local; face the player (turn body over 0.4 s) when within 4 m and head look-at while in dialog (the render lane's FollowCamera exposes the conversation partner); gesture library as keyframe clips on the humanoid bones for BOTH the authored Meshy rigs and the auto-rig: talk loop (hands + head, while the dialog is open), point, nod, shake, shrug, clap, cheer, think, wave — wired to the existing hooks (playOnce names gestureGreet/gestureCorrect/gestureWrong; add gestureTalk started on dialog open, stopped on close); emote sprite above the head on results (✓ ✗ ✦ ❤ ?), v3 styled; breathing/sway on every idle. The 7 Meshy walkers in citizens.js: outline + look-at + the same gesture hooks, or replace them with crowd-v2 'featured' bodies if that reads better (say which and why).
 5. TRAFFIC + TRAMS (traffic.js, train.js): headlights + tail lights at night through materials.withEmissiveGain, wheel spin, suspension bob, brake dip when stopping for the player; tram doors open at the platform stop with an interior light and a soft bell (audio.play exists), passengers silhouettes inside (cheap quads).
 6. POTATO tier: same silhouettes, no outline pass, blob shadows stay.
Verification bar: 12+ close-ups at 3 m of different crowd walkers across 4 districts showing distinct wardrobes and faces; hero close-up with outline + look-at; a local mid-'talk' gesture with the dialog open; tram doors open at a platform at night; renderer.info per tier vs WAVE1 numbers (crowd draw calls +1 only); zero pageerrors. Report the triangle count per crowd body (min/max across gates).` },
  { key: 'quests', worktree: '/root/em-wt-quests', port: 4186, brief: `
LANE: SERVER PROGRESS, RANKINGS, THE RIDE, WAYFINDING, FEEDBACK. The Convex functions exist on branch play/server (/root/em-wt-server/convex/worldProgress.ts + schema.ts — read them; they are NOT deployed yet, so your client must treat "function not found" / non-2xx as offline and keep working from localStorage): worldProgress:load {sessionToken} → {xp, rank, stamps, weekXp, state, stateVersion, updatedAt} | null; worldProgress:save {sessionToken, xp, xpDelta, rank, stamps, state (JSON string <= 64 KB), stateVersion} → {accepted, xp, stateVersion, weekXp}; worldProgress:leaderboard {sessionToken, scope:'all'|'week', limit<=50} → {board:[{position,name,xp,rank,stamps,isYou}], you:{position,xp}|null, week}. The game reaches Convex through nginx: POST /api/query and /api/mutation with JSON {path:'worldProgress:load', args:{...}, format:'json'} (see how index.html's gate calls studentAuth:myVerification; the SPA's src/lib or contexts show the mutation shape — grep '/api/mutation' in /root/englishmetro/src). Session token: localStorage em-student-session.sessionToken.
Build:
 1. NEW src/net.js + progress.js remote: load on BEGIN (parallel with the world build; do not delay BEGIN), merge = max laps per circuit, union of done/warm/street sets, max xp, union stamps/badges, max streak; then write-through with a 2 s debounce after any progress write, flush on visibilitychange→hidden and pagehide via navigator.sendBeacon('/api/mutation', Blob JSON) (verify the proxy accepts it; fall back to fetch keepalive), stateVersion increments per save, xpDelta = XP earned since the last accepted save; on accepted:false reload + re-merge; a small 'saved ✓ / offline' indicator in the journal header; namespaced localStorage cache per student stays the offline truth. Never block play on the network.
 2. RANKINGS tab in the Metro Pass journal: all-time / this-week toggle, top 20 + 'you' row, rank badge + stamps, first name + initial only (that is all the server returns), refresh on open, empty/offline states in v3 style. Also a compact 'weekly #N' chip in the HUD rank pill when ranked.
 3. SESSION SUMMARY card (v3): shown when the journal opens after >= 8 min of play or right after a stamp: XP this session, first-try accuracy, dialect words learned (street explain fields), 3 review items due, next suggested stop. DAILY GOAL ring (120 XP) on the XP chip with a 'goal met' fanfare (+40) and streak display in Passport (progress.js already has streak data).
 4. BOARD THE TRAM (NEW src/ride.js, extend main.js rideTo): replace the 0.48 s fade-teleport with a ride: player auto-walks to the platform edge (or just turns to face the tram), a tram from trains.js pulls in (use its existing route/position API; if none is near, spawn a ride-only car), doors open (chars lane adds door animation — call trains.setDoors if present, else skip), camera cuts to a fixed interior mount, 4-6 s transit with the world streaming past (raise fog slightly), an 'Arriving at <district>' card with 3 dialect words + IPA from the district's chatter/streetExercises and the concept hint of the local not yet helped, then doors open and the render lane's arrival dolly takes over (window.__EM.rideTo / FollowCamera.startDolly already exist — read main.js rideTo and player.js startDolly). Must degrade to the old fade if anything is missing. Keep canRide radius 24 m.
 5. WAYFINDING (NEW src/wayfinding.js): screen-edge chevron to the nearest unhelped ❗ local (project to NDC, clamp to the screen edge, v3 style, hides when the target is on screen); a faint ground trail (instanced quads along the straight line, fading with distance) toggled from the objective chip for the current beat; a glowing platform disc mesh at every stop so the 24 m ride zone is visible; metro list pins 'You are here' + 'Next stop on this line' at the top with stop counts per line; desktop map shows names for stamped/visited stations always.
 6. FEEDBACK: per-question first-try dots exist; add a 0.6 s confetti burst (InstancedMesh, ~120 quads with gravity) on a passed drill around the local and on stamp; distinct correct/wrong/round/stamp/rank sounds (audio.js has a synth; keep it small); XP counter tween exists — make sure it never overlaps the objective chip on mobile.
Verification bar: scripted playthrough proving: offline mode works with no server (route /api/query|mutation to 404 → game plays, indicator says offline); with a stubbed server (Playwright route fulfilling load/save/leaderboard) the merge rules hold (seed a remote state with higher laps and a local state with more XP → both survive), saves debounce (count requests), pagehide flushes; rankings tab renders both scopes; ride sequence screenshots (platform → interior → arrival card → dolly); chevron + trail visible; summary card; mobile --mobile pass with no overlaps; zero pageerrors.` },
  { key: 'content', worktree: '/root/em-wt-content', port: 4187, brief: `
LANE: LEARNING CONTENT — the exercises must make sense and teach. Read REVIEW-findings.md content-* and gameplay-* findings first, then WAVE1-game.md (the lint gate and what was purged: the bank is now 190 items, all 3-option, 0 offenders; levels A1 41 · A2 92 · B1 43 · B2 14 · C1 0; pronouns 7, comparatives 8, gerund_infinitive 8, conditionals 9, modals 9, questions_negation 9, word_order 12). Tools: node public/play/tools/lint-grammar-bank.mjs (must stay at 0 offenders after every change; extend it if you add fields). Audience: adult Polish learners of English at a private school; explanations name the Polish trigger where it exists (dla/do, zależeć od, aktualnie/actually...). Be conservative: a wrong 'correct answer' is the worst defect this game can have — when unsure, do not ship the item; list it in <worktree>/CONTENT-REVIEW.md for the owner.
Build (data first, minimal code):
 1. WARM-UP EXPLAINS: add 'explain' (<= 160 chars, accurate, dialect-literate) and an optional 'pl' gloss to every one of the 132 sampleExercises in zones.json; render both in ui.js's warm-up branch on right AND wrong (mirror openStreetDialog) — that is one of your two allowed code hunks.
 2. THIRD LOCAL PER DISTRICT: author npcs[2] for all 44 districts (name fitting the place, a real job that suits the district, a one-line in-dialect greeting with real lexis, standard spelling) so districtCastFor's template padding never fires; keep voiceId null (no baking possible here) and note that 44 clips are still to bake; make sure no district repeats a name between quest locals and its street roster.
 3. STREET ROSTER + ITEMS: a 4-name street roster per district (names matching the streetExercise roles, no clash with the quest locals) in zones.json, and grow streetExercises from 4 to 8 per district (one overheard line, one question, one accurate explanation, standard spelling, equalised option lengths, varied prompts, balanced answerIndex) — 176 new items; dialects.js streetLocalFor reads the roster (your file).
 4. PER-DISTRICT LEARNING OBJECTIVE: objective {concept, dialectPoint, canDo} for all 44 (e.g. Pearly Court: question tags + rhyming slang, 'I can follow a Cockney market seller'); grammar.js assignGrammar uses objective.concept for lap 0 and rotates from it (your second allowed code hunk); the objective chip already shows beats — expose canDo for the quests lane via zone data only.
 5. BANK TOP-UP: bring every concept to >= 20 items with 3 parallel options that differ only in the target feature, exactly one plausible learner error per distractor, an L1-contrast explanation, honest CEFR tags (A1 short/high-frequency, A2 everyday, B1 complex sentences, B2 nuance); add >= 8 B2 items per concept so lap escalation has content; 2-3 'validIn' dialect-contrast items for districts whose grammar differs (Southern double modals us_southern, habitual be us_aave, Scots 'I'm no wanting' sco_*, Newfoundland 'I comes', NZ/AU 'yeah nah' as discourse) so localValid finally fires there. Run the lint after every batch; 0 offenders.
 6. CHATTER + LABELS: replace eye-dialect spellings in chatter.json with standard spelling (keep the lexis: wicked, frappe, bubbler, criss, liming, gannin hyem → 'going home'?? keep dialect WORDS, fix only phonetic respellings like Pahk the cah / heah / Fuhgeddaboudit / Wuh gine on); check the two South African display names the game lane changed read well in every surface; line colour words consistent (Isles cyan, Liberty purple, Sunward pink) in chatter lines.
 7. Write public/play/tools/content-report.mjs that prints per-district completeness (npcs 3/3, street 8/8, warm-up explains n/3, objective yes/no) and per-concept/level counts; include its output in WAVE2-content.md.
Verification bar: lint 0 offenders; content-report shows 44/44 on every column; a headless probe opening 6 warm-ups (2 per line) shows the explain on a wrong answer; a drill in us_southern shows a validIn heads-up; screenshots of each; zero pageerrors.` },
]

const REPORT = {
  type: 'object',
  properties: {
    lane: { type: 'string' }, branch: { type: 'string' },
    commits: { type: 'array', items: { type: 'string' } },
    summary: { type: 'string' },
    findings: { type: 'array', items: { type: 'object', properties: { id: { type: 'string' }, status: { type: 'string', enum: ['fixed', 'partial', 'not-done', 'not-reproducible'] }, before: { type: 'string' }, after: { type: 'string' }, note: { type: 'string' } }, required: ['id', 'status', 'note'] } },
    newFiles: { type: 'array', items: { type: 'string' } },
    filesTouched: { type: 'array', items: { type: 'string' } },
    metrics: { type: 'string' },
    screenshots: { type: 'array', items: { type: 'string' } },
    knownGaps: { type: 'array', items: { type: 'string' } },
    mergeNotes: { type: 'array', items: { type: 'string' } },
    ownerReview: { type: 'array', items: { type: 'string' }, description: 'decisions or content the owner should look at' },
  },
  required: ['lane', 'branch', 'commits', 'summary', 'findings', 'filesTouched', 'metrics', 'screenshots', 'knownGaps', 'mergeNotes'],
}

phase('Build')
const results = await parallel(LANES.map((l) => () => agent(
  `${SHARED}\n\nYOUR LANE: ${l.key}\nYOUR WORKTREE: ${l.worktree} (branch play/${l.key})\nYOUR SERVER: http://127.0.0.1:${l.port}/play/\n${l.brief}\n\nReturn the structured report with lane="${l.key}".`,
  { label: `build:${l.key}`, phase: 'Build', schema: REPORT, effort: 'high' },
)))
return results.filter(Boolean)
