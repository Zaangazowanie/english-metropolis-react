export const meta = {
  name: 'em-play-revamp-wave3',
  description: 'Wave 3 of the englishmetro.com/play revamp: runtime hardening + health beacon, delivery/caching, polish pass (murals, station sign in arrival frame, emissive hierarchy), perf across tiers; max 3 agents',
  phases: [{ title: 'Build', detail: 'runtime / delivery / polish lanes' }],
}

const SHARED = `
You are a wave-3 builder lane on "English Metropolis" (englishmetro.com/play, vanilla three.js r182, no bundler). Waves 1 and 2 are merged into your base branch (read WAVE1-*.md and WAVE2-*.md in the worktree root first, then the runtime-* and walkthrough-* findings in /tmp/claude-0/-root/73bb1e95-7894-4a1d-82d2-c947c0590d3c/scratchpad/REVIEW-findings.md). The no-babysitting bar (owner's standing rule): anything that can fail must be detected, reported and where possible self-healed, never discovered weeks later by the owner. Product rules: locals not teachers; v3 design system; 13 px floor; keep the signup gate.
YOUR WORKTREE / PORT are below. Commit early and often (no push). Do not touch /root/englishmetro or other lanes' worktrees.
CPU DISCIPLINE (hard): every headless probe through `/root/ricky-estate-2026-09-01/em-play-revamp/probe-run.sh -- node ...` (queue + 1.5-core cgroup cap + steal gate; exit 75 means wait), 960x540 + TIER=potato for iteration, 1440x900 only for cited evidence, step() only the frames you need, close the browser on SIGTERM/uncaught; check sar -u first and do code work while steal > 40%. Tour script: /tmp/claude-0/-root/73bb1e95-7894-4a1d-82d2-c947c0590d3c/scratchpad/tour-play.mjs (SMALL=1 for 960x540).
No npm install, no new deps. Zero pageerrors at the end. Write <worktree>/WAVE3-<lane>.md and return the structured report.`

const LANES = [
  { key: 'runtime', worktree: '/root/em-wt-runtime', port: 4188, brief: `
LANE: RUNTIME HARDENING + HEALTH BEACON. Owns: loaders.js, audio.js, voice.js, main.js load/bootstrap section (LoadingManager, Promise.all → allSettled, retry helper, watchdog, context-lost handlers, visibilitychange), NEW src/health.js, index.html loading-screen block only, quality.js only if a tier bug is found, world.js/zones.js only for load-order (NPC bases arriving late via setNPCBases).
Findings: runtime-no-error-reporting-anywhere, runtime-load-all-or-nothing, runtime-loading-stall-no-watchdog, runtime-first-visit-9mb-before-begin (phase the load: hero + zones.json + hub first → BEGIN; NPC rigs + props stream after with fetchpriority=low; byte-weighted progress bar; rotating 'what you will do' tips), runtime-audio-no-resume-no-visibility (suspend/resume AudioContext + pause the loop when hidden; resume on gesture), runtime-voice-html-audio-outside-gesture (voice through WebAudio buffers on the unlocked context; prefetch a district's two clips on stream-in), runtime-webgl-context-lost-unhandled (panel + beacon + reload path), runtime-fixed-step-catchup-burst (cap catch-up at 4 ticks; 30 Hz sim on potato/low with interpolated crowd), runtime-npc-bases-empty-silent (assert rigged bases >= 3, visible warning + beacon), runtime-quality-change-not-applied-to-built-chunks (mark chunks dirty on tier change; rebuild nearest per frame via the city lane's queue), runtime-em-progress-parse-unguarded (verify progress.js already guards; add safeSet everywhere else), gameplay-27/content-19 note only.
HEALTH BEACON: collect {sid, ts, ua, gpu string, detectedTier, tier, renderScale, dpr, viewport, loadMs, bytes (PerformanceResourceTiming), fps p50/p95 + longest frame, chunkBuildMs p95, geometries/textures, crowd free slots, speakers/agents, districts visited, xp, audioState, voice failures by engine, errors[] from onerror/unhandledrejection/contextlost/asset(url,status)}; navigator.sendBeacon('/api/play-beacon') at BEGIN, every 60 s, on pagehide; the endpoint does not exist yet — write the nginx location + a 40-line Python appender (ndjson under /var/lib/em-play/) as files in <worktree>/ops/ with install notes (do NOT touch /etc yourself), and make the client silent when the endpoint 404s. Also write tests/play-smoke.mjs: a 90-s Playwright run asserting zero pageerrors, BEGIN reachable, draw-call/triangle budgets, speakers <= agents, geometries return to baseline after a stream cycle — to be run by the deploy script.
Verification: fault-injection probes (route a GLB to 404, to 522, to stall; route /api/query to 500 JSON) must show the game still playable or a clear Retry panel, never a dead bar; beacon payload printed; audio state after visibility toggles; zero pageerrors.` },
  { key: 'delivery', worktree: '/root/em-wt-delivery', port: 4189, brief: `
LANE: DELIVERY + CACHING + DEPLOY HYGIENE. Owns: deploy/deploy-play-*.sh (extend, do not run), NEW ops/nginx-play.conf snippet + ops/README (do NOT edit /etc yourself), a versioning step script scripts/play-version-imports.mjs, public/play/index.html import map + module preloads only, public/play/public/vendor cleanup.
Findings: runtime-cf-browser-ttl-stale-modules (rewrite the import map and every intra-src import to ?v=<git-sha> at deploy time via the script; verify the game loads from the versioned tree), runtime-glb-not-edge-cached-origin-522 (nginx location for /play/public/ with immutable long cache + include em-security-headers.conf; cache-busting by path when assets change; document the Cloudflare cache rule to add), runtime-gate-on-critical-path-no-preload (modulepreload main.js + three.module.js + three.core.js + GLTFLoader; preload zones.json), runtime-unminified-three-and-encoder-in-repo (delete draco_encoder.js from the shipped set if unused; evaluate an esbuild single-bundle build ONLY if esbuild already exists in /root/englishmetro/node_modules — no installs; otherwise keep unbundled), runtime-nginx-worker-connections-under-load (document the worker_connections/rlimit change in ops/README with the exact diff; do not apply).
Extend deploy/deploy-play-<date>.sh: run tests/play-smoke.mjs (runtime lane writes it; stub if absent) against the local tree before rsync and against the live URL after; write the FACTS-style probe rows (index 200, main.js 200 + version marker, one GLB 200 with cache headers) into ops/facts-probes.yaml for /var/lib/brain/goals.yaml.
Verification: a local versioned build served on your port loads with all imports carrying the sha; curl -I on the local python server is not nginx, so verify header logic by reviewing the snippet against the existing /etc/nginx/sites-enabled/englishmetro.com (read-only) and by a dry-run with nginx -t on a copy in /tmp if nginx is available.` },
  { key: 'polish', worktree: '/root/em-wt-polish', port: 4190, brief: `
LANE: VISUAL POLISH PASS on the merged world. Owns: kit/*.js, zones.js buildChunk/buildStationSign/buildFacadeBlock regions, world.js hub/boulevard geometry, media.js, city-life.js, materials.js only for small tuning hunks.
Tasks: (1) the 'mural on a blank side wall' (kit/facades.js ~line 486-492) reads as a glitch — a flat circle with an ink outline; replace with a painted canvas mural (2-3 palette colours, geometric/stripe/sun/wave patterns, district-themed via the signage atlas) or a bunting/ivy alternative; (2) put the station sign in the settled arrival frame (render lane note: it sits behind the player after the dolly — move the sign/platform furniture to the district side of the sidewalk ring or offset the arrival spot so both the sign and the first local are in frame; verify by projection at 3 districts); (3) route every emissive MeshBasic/MeshStandard (media screens, tickers, headlights, tail lights, litMat, shop signs) through materials.withEmissiveGain so the night hierarchy holds: screens ~1.6, lit windows ~1.3, neon ~2.2; (4) district ground/road/terrain colours to daylight albedos everywhere the city lane left navy (verify at potato where post is off); (5) wet streets on plaza/boulevard/districts at night only (uWet follows daylight); (6) z-fighting sweep at 60 m (probe screenshots looking down a boulevard); (7) shadow acne sweep on facade trims after the inset windows (normalBias per material if needed); (8) LOD/pop sweep: stream a district in/out 5 times and screenshot for pops; (9) potato tier look: same silhouettes, readable, no black sky, no missing kit; (10) mobile medium tier frame at 393x852 for overlaps between world props and HUD.
Verification: before/after screenshot pairs for each task at 1440x900 TIER=high golden + night, plus potato; renderer.info per tier within the wave-2 numbers; zero pageerrors.` },
]

const REPORT = { type: 'object', properties: { lane: { type: 'string' }, branch: { type: 'string' }, commits: { type: 'array', items: { type: 'string' } }, summary: { type: 'string' }, findings: { type: 'array', items: { type: 'object', properties: { id: { type: 'string' }, status: { type: 'string', enum: ['fixed', 'partial', 'not-done', 'not-reproducible'] }, before: { type: 'string' }, after: { type: 'string' }, note: { type: 'string' } }, required: ['id', 'status', 'note'] } }, newFiles: { type: 'array', items: { type: 'string' } }, filesTouched: { type: 'array', items: { type: 'string' } }, metrics: { type: 'string' }, screenshots: { type: 'array', items: { type: 'string' } }, knownGaps: { type: 'array', items: { type: 'string' } }, mergeNotes: { type: 'array', items: { type: 'string' } }, opsToApply: { type: 'array', items: { type: 'string' }, description: 'nginx/Cloudflare/cron changes for Ricky to apply by hand' } }, required: ['lane', 'branch', 'commits', 'summary', 'findings', 'filesTouched', 'metrics', 'screenshots', 'knownGaps', 'mergeNotes'] }

phase('Build')
const results = await parallel(LANES.map((l) => () => agent(`${SHARED}\n\nYOUR LANE: ${l.key}\nYOUR WORKTREE: ${l.worktree} (branch play/${l.key})\nYOUR SERVER: http://127.0.0.1:${l.port}/play/\n${l.brief}\n\nReturn the structured report with lane="${l.key}".`, { label: `build:${l.key}`, phase: 'Build', schema: REPORT, effort: 'high' })))
return results.filter(Boolean)
